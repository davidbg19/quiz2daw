
    <form method="get" action="/admin/cuestionarios/<%= cuestionario.id %>/quizes/<%= quiz.id %>/answer">
      <p>Pregunta: <%= quiz.pregunta %> </p>
      <input type="text" name="respuesta" value="Responda aquí"/>
      <input type="submit" value="Enviar">
    </form>
<%if(session.role==1) {%>
<p><a href="/admin/cuestionarios/<%= cuestionario.id %>/quizes/<%= quiz.id %>/comments/new"><button>Nuevo comentario</button></a></p>
<%}%>
<p><strong>Comentarios:</strong></p>

<%for(index in quiz.Comments){%>
	<%if(quiz.Comments[index].publicado || session.role==1) {%>
		<p>- <%=quiz.Comments[index].texto%></p>
		<%if(!quiz.Comments[index].publicado && session.role==1) {%>
			<a href="/admin/cuestionarios/<%= cuestionario.id %>/quizes/<%= quiz.id %>/comments/<%= quiz.Comments[index].id%>/publish"><button>Publicar</button></a>
		<%}%>
		</p>
	<%}%>
<%}%>

